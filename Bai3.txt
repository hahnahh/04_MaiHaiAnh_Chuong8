def kiem_tra_so_armstrong(n):
    # Chuyển số thành chuỗi để duyệt từng chữ số
    digits = str(n)
     # Tính tổng lũy thừa bậc 3 của các chữ số
    tong = sum(int(d)**3 for d in digits)
      # Kiểm tra xem có bằng chính nó không
    return tong == n

