
ds_so = [10, 25, 30, 45, 100, 2024]

with open('so_nguyen.txt', 'w', encoding='utf-8') as f:
    for so in ds_so:
        dong = str(so) + '\n'
        f.write(dong)

print("Đã ghi danh sách số vào file 'so_nguyen.txt' thành công.")
