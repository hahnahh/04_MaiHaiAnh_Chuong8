#thu_vien_chung-xu_ly_so.py
import math
def kiem_tra_so_nguyen_to(so):
    if so < 2:
        return False
    for i in range(2, int(math.sqrt(so)) + 1):
        if so % i == 0:
            return False
    return True
#du_an_chinh-kiem_tra.py
import sys
sys.path.append('../thu_vien_chung')
import xu_ly_so
so = 13
if xu_ly_so.kiem_tra_so_nguyen_to(so):
    print(f"{so} là số nguyên tố")
else:
    print(f"(so) không phải số nguyên tố")

